---
interface Props {
  class?: string;
  as?: 'div' | 'section' | 'article';
}
const { class: className = '', as: Tag = 'div' } = Astro.props;
---

<Tag class:list={['mx-auto w-full max-w-container px-[var(--container-padding)]', className]}>
  <slot />
</Tag>
